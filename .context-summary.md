## Goal
- 按用户批准的路线图改造 ChemLab-G9 界面（视觉 + 游戏化激励层），随后完善学习内容/习题编排，生产输出 Day01–10 内容；全程保持"单 HTML 文件、iPad 离线打开"的约束。

## Constraints & Preferences
- 最终交付必须是单个自包含 HTML（`dist/ChemLab-G9.html`），通过 AirDrop 传 iPad，Safari 离线打开，无构建/无网络/无外部 CDN。
- 原生 HTML/CSS/JS（无框架、无打包器），内容存 `content/` + `quiz/`，渲染逻辑在 `src/js/app.js`。
- 动机层（连击/连续天数/成就/薄弱点）必须全部基于 `localStorage` 本地可判定，不依赖账号或网络。
- 内容遵循教育原则：事实 vs 结论分开、控制变量、"氧气助燃不燃"等科学准确性；实验必须标注 `safety: "supervised"`。
- 用户已配置 GLM API（`glm-4-flash` 主用 / `glm-4.7-flash` 备用）到 opencode.json，重启后生效。

## Progress
### Done
- CSS 全面视觉改造（`src/css/app.css` 整体重写）：渐变 hero、统计条 `stats-strip`、成就墙 `badge-wall`，薄弱点标签 `weak-tags`，模块色 `mod-0..mod-7`，完成勾选 `day-check`，可展开模块块 `details.mod-block`，卡片内迷你折线 `mini-chart`，练习进度条 `quiz-progress`，选项反馈态（`is-correct/is-wrong/is-marked`、fade 动效 `fb-ok/fb-ko`、禁用 radio），空气成分图例块，暗色模式。
- `app.js` 激励层：`LS_STATS`（`chemlab-g9:v3:stats`）、`getStreak()`（基于 attempts 日期逐日回溯）、`getWeakTopics()`（从 review 队列按题目 `topic` 聚合前 5）、`ACHIEVEMENTS` 8 个徽章 + `evaluateAchievements()`（首次完成/累计5天/满分/含挑战满分/连对5/连续3天/连续7天/错题清零）、`miniChart()` 增长折线、`getStats/saveStats`。
- `app.js` 首页重写：hero 渐变 + 进度条 + 连续天数/最高连对/待复习 chips + 薄弱点标签；成就墙；模块进度改为可展开 `<details>` 列表（环形图 + 每日状态）；day 卡片按模块着色、完成显示对勾 + 迷你折线 + "最佳 X/N · 尝试 N 次"。
- `app.js` 课程页：sections body 支持对象段落 `{text, kind:"takeaway"|"note"}`（渲染为 `.takeaway`/`.note-block` 高亮块）；题目渲染加入 `q-tags`（难度 + 知识点徽章）；quiz 区顶部题号进度条（`#qp-fill`/`#qp-text`，change 时更新）。
- `app.js` 提交逻辑增强：正确项 `is-marked` 高亮、选错项 `is-wrong` 标红、radio 禁用、反馈动效、连击计数并持久化 `stats.bestCombo`、结果显示连对题数；复习页同样处理并对"错题清零"判定 `reviewCleared` 成就。
- 新增配图渲染器 `figAirComposition()`（空气成分环形图 + 图例），注册进 `renderFigure`；配套 CSS。
- `app.js` 底部返回链接（`.back-home`）：学习页和复习页底部均加 `← 返回首页` 按钮，品牌色实心按钮，居中，悬停动效。
- `content/manifest.js`：`knownTitles` 扩展 Day01–18；`readyDays` = 01–18。
- `scripts/validate-content.mjs`：适配新 schema（body 允许 `{text, kind}` 且 kind ∈ takeaway/note；校验 `topic` 非空字符串）。
- 内容产出：`content/days/day-01.js` 已扩充（10 sections、10 quiz 题、6 checkpoint）；`content/days/day-02.js` 保持原样；`content/days/day-03.js`（科学探究方法，6 题）；`content/days/day-04.js`（空气，含 `air-composition` 配图，8 题）；`content/days/day-05.js`（氧气的性质，8 题）；`content/days/day-06.js`（氧气的制取，9 题）；`content/days/day-07.js`（单元复习：化学变化与空气，10 题）；`content/days/day-08.js`（分子和原子，8 题）；`content/days/day-09.js`（原子的结构，9 题）；`content/days/day-10.js`（元素，9 题）；`content/days/day-11.js`（离子，5 节）；`content/days/day-12.js`（元素周期表简介，6 节）；`content/days/day-13.js`（化学式与化合价，5 节）；`content/days/day-14.js`（相对分子质量计算，6 节）；`content/days/day-15.js`（单元复习：物质构成的奥秘，5 节）；`content/days/day-16.js`（水的组成，5 节）；`content/days/day-17.js`（水的净化，7 节）；`content/days/day-18.js`（爱护水资源，5 节）。
- Quiz 产出：`quiz/day-01.js` 至 `day-18.js` 全部完成（10/16/6/8/8/9/10/8/9/9/8/6/7/7/8/8/7/6 题）。
- `tests/smoke.mjs`：适配新首页 + Day11-18 题数断言。
- `README.md` 更新至 V3.2，新增 Day01–18 发布表。
- GLM API 配置：`/root/.config/opencode/opencode.json` 已写入 `glm` provider（`glm-4-flash` 主用 / `glm-4.7-flash` 备用），用户需重启 opencode 生效。
- Git 提交推送：`fde63aa`（Day11-18 内容+quiz）等。
- 流水线绿灯：`node scripts/validate-content.mjs && node scripts/build-single.mjs && node tests/smoke.mjs` 全部通过。
- 构建产物：202 KB，18 天内容已内联。

### In Progress
- (none)

### Blocked
- (none)

## Key Decisions
- 单文件构建流：`scripts/build-single.mjs` 按次序内联 manifest → 所有 ready 天数内容 → 对应 quiz → `app.js`，输出 `dist/ChemLab-G9.html`。
- 动机数据模型：`day-XX` 记录为 `{attempts:[{score,total,answers,completedAt}], best}`；review 队列为 `{day, questionIndex, prompt, answeredAt}`；附加统计存 `chemlab-g9:v3:stats`。
- 模块配色 `mod-N` 与 manifest `modules` 数组索引一一对应。
- 内容生产顺序：Day01–03 开启化学之门 → Day04–08 我们周围的空气 → Day09–15 物质构成的奥秘 → Day16–18 自然界的水。
- 校验/构建/冒烟测试作为发布门禁。
- ASCII 引号问题：内容文件和 quiz 文件中中文短语误用 ASCII `"` 导致 JS 解析失败，改用中文全角引号 `【】` 或文字表述解决。

## Next Steps
1. 继续 Day19–30 内容生产（化学方程式、碳和碳的氧化物、燃料及其利用、综合提升）。

## Critical Context
- Git：当前分支 `main`，最新提交 `fde63aa` 已推送，与 `origin/main` 同步。
- GitHub Pages 已启用：https://Momo2015-AI.github.io/ChemLab-G9/ （推 main 后约 1–2 分钟自动更新）。
- 本地预览服务器仍在运行：python3 http.server 8080 → https://8080-6833d53114eaa34b.monkeycode-ai.online （终端 ID `term_1786008432399_7`）。
- 教育约束："氧气不可燃、仅助燃"；答案下标字符串不得越界；解析必须足够支撑正确项；实验安全标注意见。
- 新 body schema 中 `typeof p === "object"` 时为段落对象，validator 强制执行。
- opencode.json 中已添加 GLM provider，用户需重启 opencode 使配置生效。

## Relevant Files
- `src/js/app.js`：全部界面/激励/配图逻辑。
- `src/css/app.css`：完整视觉系统。
- `content/manifest.js`：Day01–18 标题 + readyDays 01–18。
- `content/days/day-01.js` 至 `day-18.js`：已推送。
- `quiz/day-01.js` 至 `day-18.js`：已推送（10/16/6/8/8/9/10/8/9/9/8/6/7/7/8/8/7/6 题）。
- `scripts/validate-content.mjs`：body 对象 + topic 校验已适配。
- `scripts/build-single.mjs`：单文件内联构建器。
- `tests/smoke.mjs`：构建产物 DOM 桩测试。
- `dist/ChemLab-G9.html`：当前构建产物（202 KB，18 天内容已内联）。
- `/root/.config/opencode/opencode.json`：已添加 GLM provider（`glm-4-flash` / `glm-4.7-flash`）。
