# ChemLab-G9 V3.1

面向暑假 30 天自学的九年级化学（人教版上册）iPad 单页学习系统。

## 当前阶段

V3.1：在 Day01 精品样章基础上，补齐首页导航、学习进度可视化与跨天错题复习队列，为 Day02--30 的批量内容生产打好可复用的基础设施。

## 本地预览

直接在浏览器打开 `index.html` 即可。第一阶段不依赖构建工具或网络服务；不传参数时展示首页导航，`?day=01` 直接进入指定天数。

## 主要功能

- **首页导航**：`content/manifest.js` 维护 30 天清单（标题、所属模块、是否已可学习），首页按此渲染学习日网格；未开发的天数点击后会提示“开发中”，不会报错或空白。
- **进度可视化**：首页展示总体进度条和每天的完成状态（未开始 / 已完成 · 分数）。
- **本地学习记录**：练习结果保存在 `localStorage`，键名以 `chemlab-g9:v3:` 开头；刷新或重新打开浏览器后进度不丢失。
- **跨天错题复习队列**：每次提交测验后，答错的题目会汇总进 `chemlab-g9:v3:review`，首页会提示“有 N 道题值得复习”，为后续复习日（Day07/15/23/30）预留数据基础。
- **实验安全标注**：需要成人陪同的实验步骤会在标题旁显示醒目的“⚠️ 需成人陪同”徽章，而不只是埋在正文里。
- **情境自测**：每天可选配 `checkpoint`（用原生 `<details>/<summary>` 实现，无需额外脚本、天然支持键盘和触控），供学生先判断后看解析，不计入测验分数。

## 目录

- `docs/`：产品、课程、教学与数据规范，另有 `PARENT_GUIDE.md` 给家长的简短使用说明
- `src/`：界面样式、交互逻辑与内容加载 / 路由代码
- `content/manifest.js`：30 天清单与导航数据
- `content/days/`：每天的学习内容数据
- `quiz/`：每日练习与单元测验数据
- `assets/`：图片、图标、音频及字体资源
- `tests/`：手工验收用例说明

## 开发顺序

1. ~~Day01 精品样章（内容、界面、练习与学习记录）~~
2. ~~通用课程页面、导航与题目组件~~（首页导航、进度、复习队列已完成）
3. Day02--Day30 内容生产与单元复习（复用现有路由与组件，只需新增 `content/days/day-XX.js`、`quiz/day-XX.js`，并在 `content/manifest.js` 的 `readyDays` 中加入对应天数）
4. 综合测试、iPad 适配与离线验收
